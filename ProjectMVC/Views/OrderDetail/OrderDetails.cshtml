@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "OrderDetails";
}

<div class="">
    <table border="1" cellspacing="50" cellpadding="50" width="200">
        <tr>
            <td>Product Id</td>
            <td>Product Name</td>
            <td>Product Price</td>
        </tr>
        @foreach (var m in Model)
        {
            <tr>
                <td>@m.Id</td>
                <td>@m.Name</td>
                <td>@m.Price</td>
            </tr>
        }
    </table>
</div>
